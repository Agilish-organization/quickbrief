{
  "name": "quickbrief",
  "version": "1.0.0",
  "description": "Paste any article — get a 3-bullet summary + a tweet version",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "express": "^4.18.2"
  }
}
